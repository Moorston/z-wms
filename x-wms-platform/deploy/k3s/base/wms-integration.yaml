apiVersion: apps/v1
kind: Deployment
metadata:
  name: wms-integration
  labels:
    app: wms-integration
spec:
  replicas: 1
  selector:
    matchLabels:
      app: wms-integration
  template:
    metadata:
      labels:
        app: wms-integration
    spec:
      containers:
        - name: wms-integration
          image: wms-integration:latest
          ports:
            - containerPort: 8084
          envFrom:
            - configMapRef:
                name: wms-env
            - secretRef:
                name: wms-secrets
          resources:
            requests:
              cpu: 200m
              memory: 256Mi
            limits:
              cpu: 1000m
              memory: 1Gi
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8084
            initialDelaySeconds: 60
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8084
            initialDelaySeconds: 30
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: wms-integration
spec:
  selector:
    app: wms-integration
  ports:
    - port: 8084
      targetPort: 8084
  type: ClusterIP
